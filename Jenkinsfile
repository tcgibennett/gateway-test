node {
    stage('Example') {
        echo 'Example1 Stage'
        echo env.BRANCH_NAME
        if (env.BRANCH_NAME == 'master') {
            def changedCharts = null
            echo "Testing"
        }
    }
}